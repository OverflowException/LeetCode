#include <iostream>
#include "solution.h"

using namespace std;

int main(int argc, char** argv)
{
  if(argc != 2)
    throw runtime_error("Please input a proper path!");

  Solution s;
  cout << s.simplifyPath(argv[1]) << endl;
}

#include <stack>
#include "solution.h"
#include <iostream>


using  namespace std;

int main(int argc, char** argv)
{
  if(argc != 2)
    throw runtime_error("Invalid parameter number!");

  Solution s;
  cout << (s.isValid(argv[1]) ? "Valid" : "Invalid") << endl;
}
